---
import config from "fish:config";
import { BaseSide, Toc } from "fish:components";
const { postEntry, headings } = Astro.props;
---

<BaseSide>
  <Toc
    title={config.side.toc?.title}
    headings={headings}
    maxDepth={config.side.toc?.maxDepth}
  />
</BaseSide>
